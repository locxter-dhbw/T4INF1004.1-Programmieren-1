#include <stdio.h>
/*
int main4(int argc, char **argv) {
    // argc : Anzahl der Parameter
    // argv: Pointer auf Char Pointer => Liste der Strings der einzelnen Parameter
    // Kommandozeilenparameter
    printf("argc: %d\n", argc); // Anzeile der Parameter
    for (int i = 0; i < argc; ++i) {
        printf("%s\n", argv[i]);
    }
    // Alternativ so
    printf("%s\n", *argv);
    for (int i = 1; i < argc; ++i) {
        argv++;
        printf("%s\n", *argv);
    }

    return 0;
}*/